{
  "name": "mm-bingo",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev:app": "pnpm -F @mm-bingo/app dev",
    "build": "pnpm -F @mm-bingo/* --if-present -r --parallel build",
    "fmt": "pnpm --if-present -r --parallel fmt",
    "lint": "pnpm -F @mm-bingo/* --if-present -r --parallel lint",
    "test": "pnpm -F @mm-bingo/* --if-present -r --parallel test"
  },
  "packageManager": "pnpm@11.13.1",
  "devDependencies": {
    "node": "catalog:"
  }
}
