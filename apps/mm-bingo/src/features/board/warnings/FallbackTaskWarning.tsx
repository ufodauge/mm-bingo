import { BoardWarningBanner } from "./BoardWarningBanner";

export const FallbackTaskWarning = () => (
  <BoardWarningBanner
    messageKey="board.fallbackTaskWarning.message"
    rerollKey="board.fallbackTaskWarning.reroll"
  />
);
